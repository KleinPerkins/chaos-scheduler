<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- WKWebView requires JIT + executable memory under the hardened runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Tauri/webview loads dynamically-linked libraries not signed by us. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Outbound HTTP (reqwest, lettre, updater downloads). -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- The app binds loopback sockets (single-instance lock, /metrics, HTTP API). -->
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
